kind: ConfigMap
apiVersion: v1
metadata:
  # Must match the 'spring.application.name' property of the application
  name: springboot-ocp
data:
  application.properties: |
    sample.info-message=hello
